# Shared Python deps for the sidecar, watchdog, and tests.
# Freqtrade itself runs from its official Docker image and is NOT installed here.
anthropic>=0.39.0
psycopg[binary]>=3.1
feedparser>=6.0
requests>=2.31
python-dotenv>=1.0
pandas>=2.1
pandas-ta>=0.3.14b0
pytest>=8.0
